# Create a sample vector
sample_vec <- c("a", "c", "cc", "aaa")

# use grepl() to find out if the given pattern exists
grepl("aaa", sample_vec)

# use grepl() to find out if the given pattern exists
grepl("aaaa", sample_vec)
