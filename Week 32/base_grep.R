# Create a sample vector
sample_vec <- c("a", "c", "cc", "aaa")

# use grep() to find out if the given pattern exists
grep("aaa", sample_vec)

# use grep() to find out if the given pattern exists
grep("aaaa", sample_vec)
