# create a sample vector of numeric values
num_vec <- c(11, 12, 13, 14, 15)

# generate a random sub sample of the data
sample(num_vec, size = 3)

# generate a random sub sample of the data
sample(num_vec, size = 3)
