# Create a sample string
sample_string <- "This is a sample string. A SAMPLE STRING"

# use the 'gsub' function to replace all instances of a character
sample_string <- gsub("a", "c", sample_string)

# print to check if the 'sample_string' is updated
sample_string