# attach the 'dplyr' package
library(dplyr)

# Create a sample vector
numbers_vector <- c(19, 16, 8, 22, 41, 3)

# use the 'percent_rank' function to rank the elements of the vectors
percent_rank(numbers_vector)


