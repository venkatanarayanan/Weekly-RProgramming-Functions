# attach the 'dplyr' package
library(dplyr)

# Create a sample vector
numbers_vector <- c(19, 16, 8, 22, 41, 3)

# use the 'ntile' function to rank the elements of the vectors
ntile(numbers_vector, 2)


