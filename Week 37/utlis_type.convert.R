# create a sample vector
numbers <- c("10", "11", "12", "13", "14", "15")

# check the class of the sample vector
class(numbers)

# convert the type of the sample vector
numbers <- type.convert(numbers)

# check the class of the sample vector after conversion
class(numbers)