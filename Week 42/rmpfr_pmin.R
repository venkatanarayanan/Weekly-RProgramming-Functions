# Create a sample numeric vector
num_vec_1 <- c(2,8,21,13,56)

# Create another sample numeric vector
num_vec_2 <- c(10, 26, 5, 38, 41)

# Use pmin() to find the parallel maximum
pmin(num_vec_1, num_vec_2)