# attach the stringr package
library(stringr)

# Create a sample string
sample_string <- "The day is Sunday"

# use 'str_ends()' to check the ending pattern of the string
str_ends(sample_string, "Sunday")
